import styled from 'styled-components';

export const MainLayout = styled.main<{ theme: string }>`
  height: 92%;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
`;
